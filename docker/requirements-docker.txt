# Pinned runtime deps for the Grand Challenge submission container.
# Matches the official docker-template's minimal stack; PyTorch 2.9 + CUDA 12.6
# supports both the submission T4 (sm_75) and our local 5090 (sm_120).
torch>=2.6.0
monai==1.3.2                  # match training env — 1.5 dropped SwinUNETR(img_size=)
einops
SimpleITK>=2.3.0
itk>=5.3                      # MONAI's ITKReader uses `import itk`, not SimpleITK — needed for .mha input
nibabel
numpy>=1.26,<2.0              # MONAI 1.3.2's compute_shape_offset uses ndarray.ptp() which numpy 2 removed
scipy
pandas>=2.2
scikit-learn>=1.4
lifelines>=0.29
transformers>=4.40
tokenizers
omegaconf
pytorch-lightning>=2.3
